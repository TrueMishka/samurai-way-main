import {ActionTypes, FriendsType} from "./state";

type StateType = {
    friends: FriendsType[]
}

const sidebarReducer = (state: StateType, action: ActionTypes) => {

    return state
}

export default sidebarReducer